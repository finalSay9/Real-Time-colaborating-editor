# Persist data to disk so the queue survives a Redis restart
appendonly yes
appendfsync everysec

# Evict least-recently-used keys when memory is full
# rather than crashing or refusing writes
maxmemory 256mb
maxmemory-policy allkeys-lru

# Save a snapshot every 60 seconds if at least 1 key changed
save 60 1